version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query.sql"
    schema: "../migrations"
    gen:
      go:
        package: "sqlcdb"
        out: "internal/sqlcdb"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_db_tags: false
        emit_empty_slices: true
